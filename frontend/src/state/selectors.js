export const selectStreaming = (s) => s.streaming;
export const selectSessionId = (s) => s.sessionId;
export const selectStartedAt = (s) => s.startedAt;

export const selectSamples = (s) => s.samples;
export const selectLastSample = (s) => s.lastSample;

export const selectWindows = (s) => s.windows;
export const selectCurrentLabel = (s) => s.currentLabel;
export const selectCurrentScore = (s) => s.currentScore;